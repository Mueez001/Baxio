# Optional: backend endpoint to receive chatbot intake docs.
# If not set, chatbot still works and stores docs locally in the browser.
VITE_CHATBOT_WEBHOOK_URL=https://your-api.example.com/chatbot-intakes
